import { ReactNode } from "react";

export type IconButtonProps = {
    children?: ReactNode,
    background?: string,
    color?: string,
    borderColor?: string,
    borderSize?: string,
    upside?: boolean,
}